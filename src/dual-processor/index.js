import React from "react";
import Module from "../module";

const DualProcessor = ({ className }) => {
  return <Module name="Dual Processor" className="dual-processor"></Module>;
};

export default DualProcessor;
